# Domain redirects (non-www → www, .de/.li/.at → .ch) are handled via
# Cloudflare Pages → Custom Domains settings — not needed here.
#
# Add path-based redirects below if needed in the future, e.g.:
# /old-page    /new-page    301
